# 小红书宠物健康选题自动化实施方案

## 1. 当前状态

当前项目已经具备基础能力：

- 已部署本地 n8n：`D:\AUnityProject\RedBook\n8n-runtime`
- 已有 Python 抓取脚本：`scripts/fediaf_crawler.py`
- 已有中文翻译脚本：`scripts/translate_fediaf_to_zh.py`
- 已有一键自动化脚本：`scripts/fediaf_automation.py`
- 已抓取并清理 FEDIAF 资料：
  - 42 个保留网页
  - 35 个保留 PDF
  - 对应中文翻译文件

但目前 n8n 还没有真正接入流程。实际工作仍由 Python 脚本完成。

## 2. 目标

把当前项目升级成一个“宠物健康小红书选题自动化系统”。

目标流程：

```text
定时触发
  ↓
自动抓取权威宠物健康资料
  ↓
翻译成中文
  ↓
提取重点
  ↓
判断是否适合小红书
  ↓
生成选题
  ↓
写入选题库
  ↓
通知你查看
```

## 3. 技术分工

```text
Python：负责抓取、PDF 解析、翻译、清洗
n8n：负责定时、调度、AI 处理、入库、通知
Baserow / Notion：负责选题库管理
Zotero / Obsidian：负责长期资料沉淀
```

不建议用 n8n 重写爬虫。爬虫和 PDF 解析继续用 Python 更稳定。

## 4. 推荐架构

```text
n8n Schedule Trigger
  ↓
Execute Command：运行 Python 抓取脚本
  ↓
Read File：读取 run-summary.json
  ↓
Code Node：提取新增资料
  ↓
AI Node：总结资料、生成选题
  ↓
IF Node：筛选高价值选题
  ↓
Baserow / Notion：写入选题库
  ↓
通知节点：发送每日新增选题
```

## 5. 选题库数据结构

建议先用 Baserow，后续也可以接 Notion。

### 表 1：选题库

| 字段 | 类型 | 说明 |
|---|---|---|
| title | 文本 | 选题标题 |
| pet_type | 单选 | 猫 / 狗 / 猫狗通用 / 兔 |
| topic | 单选 | 营养 / 食品安全 / 标签 / 体重 / 老年宠物 / 零食 / 补剂 |
| source_org | 文本 | FEDIAF / WSAVA / FDA / AAFCO 等 |
| source_url | URL | 原始资料链接 |
| source_file | 文本 | 本地 PDF 或 Markdown 路径 |
| evidence_level | 单选 | 高 / 中 / 低 |
| xhs_angle | 长文本 | 小红书切入角度 |
| hook | 文本 | 封面标题候选 |
| summary | 长文本 | 中文摘要 |
| risk_level | 单选 | 低 / 中 / 高 |
| status | 单选 | 待评估 / 可写 / 已写 / 已发布 / 放弃 |
| score | 数字 | 综合评分 |
| created_at | 日期 | 创建时间 |

### 表 2：资料来源

| 字段 | 类型 | 说明 |
|---|---|---|
| title | 文本 | 资料标题 |
| org | 文本 | 来源机构 |
| url | URL | 原文链接 |
| local_path | 文本 | 本地文件路径 |
| content_type | 单选 | 网页 / PDF / 指南 / 新闻 |
| translated_path | 文本 | 中文翻译路径 |
| useful | 勾选 | 是否适合内容创作 |
| notes | 长文本 | 备注 |

## 6. 第一阶段：接入 n8n 定时运行

### 目标

让 n8n 每天或每周自动运行现有 Python 自动化脚本。

### n8n 节点

```text
Schedule Trigger
  ↓
Execute Command
  ↓
Read Binary/File
  ↓
Send Notification
```

### Execute Command 命令

```powershell
python D:\AUnityProject\RedBook\scripts\fediaf_automation.py --full
```

如果不想每次全量抓取，后续可改成增量模式。

### 输出

读取：

```text
D:\AUnityProject\RedBook\data\fediaf-automation-logs\latest-run.json
```

通知内容：

```text
FEDIAF 自动抓取完成
页面数量：xx
PDF 数量：xx
翻译状态：成功 / 失败
日志路径：xxx
```

## 7. 第二阶段：新增资料筛选

### 目标

不是所有资料都入库，只保留适合小红书宠物健康内容的资料。

### AI 判断规则

让 AI 按以下维度评分：

| 维度 | 分值 |
|---|---|
| 是否宠物健康相关 | 0-5 |
| 是否能转化成小红书选题 | 0-5 |
| 是否有权威来源价值 | 0-5 |
| 是否容易讲给普通养宠人 | 0-5 |
| 合规风险是否可控 | 0-5 |

总分大于等于 16，进入选题库。

### Prompt 示例

```text
你是小红书宠物健康内容策划。
请判断下面资料是否适合做小红书宠物健康选题。

要求：
1. 面向普通养宠人，不面向兽医。
2. 优先选择猫狗营养、食品安全、标签、体重、老年宠物、零食、补剂相关内容。
3. 不要选择行业政策、年报、会议、包装、环保、燃料法规。
4. 输出 JSON。

字段：
title
pet_type
topic
summary
xhs_angle
hook
evidence_level
risk_level
score
reason
```

## 8. 第三阶段：写入选题库

### 推荐方案

优先使用 Baserow。

原因：

- 类 Airtable，适合结构化选题库
- 支持 API
- 可以自托管
- n8n 容易通过 HTTP Request 写入

### n8n 写入逻辑

```text
AI 输出 JSON
  ↓
Code Node 校验字段
  ↓
IF score >= 16
  ↓
HTTP Request 写入 Baserow
```

### 去重规则

按以下字段去重：

```text
source_url + title
```

如果已经存在：

```text
更新 summary / score / status
```

如果不存在：

```text
新增选题
```

## 9. 第四阶段：通知和人工审核

### 通知内容

每天抓取后发送：

```text
今日新增候选选题：5 条

1. 猫狗吃主粮还需要补维生素吗？
评分：21
来源：FEDIAF
风险：低

2. 老年犬营养到底要注意什么？
评分：19
来源：FEDIAF
风险：中
```

### 审核方式

初期建议人工审核，不要全自动发布。

状态流转：

```text
待评估 → 可写 → 已写 → 已发布
        ↓
       放弃
```

## 10. 第五阶段：扩展更多来源

第一批来源建议：

| 来源 | 用途 |
|---|---|
| FEDIAF | 欧洲宠物食品营养指南 |
| WSAVA | 全球小动物兽医协会指南 |
| FDA Pet Food | 宠物食品安全、召回 |
| AAFCO | 营养标准、标签 |
| Tufts Petfoodology | 宠物营养科普 |
| Cornell / UC Davis | 兽医大学资料 |

扩展方式：

```text
每个来源一个 Python 抓取器
n8n 统一调度
最终写入同一个选题库
```

## 11. 实施顺序

### 第 1 步：跑通 n8n 调用 Python

验收标准：

- n8n 能手动执行 workflow
- 能运行 `fediaf_automation.py`
- 能生成日志
- 失败时能看到错误

### 第 2 步：接入定时任务

验收标准：

- 每天/每周自动运行
- 自动生成 latest-run.json
- 自动通知执行结果

### 第 3 步：接入 AI 选题筛选

验收标准：

- 能从中文 Markdown 中提取摘要
- 能输出结构化 JSON
- 能给选题打分

### 第 4 步：接入 Baserow / Notion

验收标准：

- 高分选题自动入库
- 重复资料不会重复创建
- 能按状态筛选选题

### 第 5 步：做发布工作流

验收标准：

- 每周自动汇总可写选题
- 自动生成标题候选
- 自动生成内容大纲
- 人工确认后进入创作阶段

## 12. 风险和注意事项

### 1. 不要完全自动发布

宠物健康内容有合规和科学准确性风险。AI 只能辅助，不应该直接发布。

### 2. 要保留来源

每个选题必须保留：

```text
原文 URL
本地文件
中文摘要
证据等级
```

### 3. 抓取要克制

需要遵守：

```text
robots.txt
crawl-delay
低频抓取
不绕过登录或反爬
```

### 4. AI 输出要结构化

所有 AI 节点都要求输出 JSON，避免后续入库失败。

## 13. 最小可行版本

最小版本只做这 4 件事：

```text
n8n 定时触发
  ↓
运行 FEDIAF Python 自动化脚本
  ↓
AI 从中文资料里生成候选选题
  ↓
写入选题库并通知你
```

这个版本跑通后，再扩展更多网站和更复杂的内容生产流程。

## 14. 优化后的第一步：先生成本地候选选题 JSONL

在接入 n8n、Baserow 或 Notion 之前，先增加一个 Python 中间层：

```text
translation-index.json
  ↓
读取中文 Markdown
  ↓
生成 source-items.jsonl
  ↓
生成 topic-candidates.jsonl
  ↓
生成 eligible-topic-candidates.jsonl 和 review-report.md
  ↓
导出 baserow-topic-candidates.csv 和 n8n-items.json
  ↓
人工抽查候选质量
  ↓
再交给 n8n 入库和通知
```

当前脚本：

```powershell
python scripts\generate_topic_candidates.py
```

输出：

```text
data\topic-pipeline\source-items.jsonl
data\topic-pipeline\topic-candidates.jsonl
data\topic-pipeline\eligible-topic-candidates.jsonl
data\topic-pipeline\review-report.md
data\topic-pipeline\summary.json
data\topic-ingestion\baserow-topic-candidates.csv
data\topic-ingestion\baserow-topic-candidates.json
data\topic-ingestion\n8n-items.json
data\topic-ingestion\field-mapping.md
```

这样做的好处：

- n8n 后续只需要消费稳定 JSONL，不需要直接解析 Markdown。
- n8n 优先读取 eligible-topic-candidates.jsonl，避免重复实现评分和风险过滤。
- 如果要直接进 n8n 或 Baserow，先运行 export_topic_ingestion.py，使用 n8n-items.json 或 baserow-topic-candidates.csv。
- review-report.md 用于人工快速抽查候选质量和高风险记录。
- 可以先验证选题质量，再决定是否写入 Baserow。
- 每条候选都保留 source_url、source_file、translated_path、content_hash、evidence_excerpt、risk_level 和 vet_boundary_required。
- 高风险内容进入人工复核，不直接进入发布队列。

导出命令：

```powershell
python scripts\export_topic_ingestion.py
```

去重 / 更新键：

```text
candidate_id
```

## 15. n8n 最小接入工作流

先不接 Baserow，也不自动发布。第一条 n8n workflow 只验证本地自动化链路：

```text
Manual Trigger
  ↓
Execute Command
  ↓
运行 scripts\n8n_topic_ingestion_smoke.py
  ↓
读取 data\topic-ingestion\n8n-items.json
  ↓
生成 data\n8n-topic-smoke\latest-summary.json
  ↓
生成 data\n8n-topic-smoke\latest-summary.md
```

workflow 导入文件：

```text
n8n-workflows\topic-ingestion-smoke.workflow.json
```

本地 n8n 2.x 默认禁用 Execute Command 节点，启动 n8n 时需要设置：

```powershell
$env:NODES_EXCLUDE='[]'
```

`n8n-runtime\start-n8n.ps1` 已加入这个环境变量。CLI 执行时如果本地 n8n 服务已经占用 `5679` task broker 端口，可以临时指定备用端口：

```powershell
$env:N8N_RUNNERS_BROKER_PORT='5689'
```

验收标准：

- n8n 可以手动执行 workflow。
- Execute Command 节点能成功运行 Python。
- 能读到 58 条可入库候选。
- 能生成 latest-summary.json 和 latest-summary.md。

当前验证状态（2026-07-08）：

- workflow 已导入本地 n8n，ID 为 `RedBookTopicIngestionSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- `data\n8n-topic-smoke\latest-summary.json` 和 `latest-summary.md` 已更新到本地时间 `2026-07-08 18:07:24`。
- 当前输出为 58 条候选，其中 34 条需要兽医边界提示；风险等级为 `中=34`、`低=24`。

这个 smoke workflow 跑通后，再把 Execute Command 后面的步骤替换或扩展为 Baserow API upsert、通知节点和人工审核入口。

## 16. 本地选题库 upsert 层

在接入真实 Baserow/Notion 凭据之前，先用本地 SQLite 验证入库逻辑：

```text
data\topic-ingestion\n8n-items.json
  ↓
scripts\upsert_topic_database.py
  ↓
data\topic-database\topic-candidates.sqlite
  ↓
data\topic-database\latest-upsert-summary.json
  ↓
data\topic-database\latest-upsert-summary.md
```

设计原则：

- `candidate_id` 是主键，用于去重和更新。
- 重复执行不会重复创建记录。
- 默认保留人工修改过的 `status`，避免把“已写 / 放弃 / 人工复核”等状态覆盖回“待评估”。
- `baserow.env.example` 只保存占位配置，真实 token 不提交。

当前脚本：

```powershell
python scripts\upsert_topic_database.py
```

n8n workflow：

```text
n8n-workflows\topic-database-upsert-smoke.workflow.json
```

当前验证状态（2026-07-08）：

- workflow 已导入本地 n8n，ID 为 `RedBookTopicDatabaseUpsertSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- 本地 SQLite 选题库已有 58 条候选。
- 重复执行结果为 `inserted=0`、`updated=0`、`unchanged=58`。
- 当前状态分布为 `待评估=58`。

下一步再把本地 SQLite upsert 替换或扩展为 Baserow API upsert，并增加通知和人工审核入口。

## 17. 本地人工审核入口和通知摘要

在 Baserow/Notion 审核表接入前，先从本地 SQLite 选题库生成一个静态审核入口：

```text
data\topic-database\topic-candidates.sqlite
  ↓
scripts\generate_review_portal.py
  ↓
data\review-portal\index.html
  ↓
data\review-portal\review-queue.json
  ↓
data\review-portal\notification-summary.md
  ↓
data\review-portal\review-actions-template.csv
```

当前脚本：

```powershell
python scripts\generate_review_portal.py
```

当前 n8n workflow：

```text
n8n-workflows\topic-review-portal-smoke.workflow.json
```

设计原则：

- 审核页面只做本地查看、筛选和证据检查，不直接自动发布。
- 通知摘要先输出 Markdown，后续可接企业微信、邮件或 Telegram。
- `review-actions-template.csv` 先作为人工状态动作模板，后续可以改成 Baserow 表格状态。
- 需要兽医边界提示的候选必须在审核时保留“建议咨询兽医”的边界表达。

当前验证状态（2026-07-08）：

- workflow 已导入本地 n8n，ID 为 `RedBookTopicReviewPortalSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- 审核入口包含 58 条待评估候选。
- 其中 34 条需要兽医边界提示。
- `index.html`、`review-queue.json`、`notification-summary.md`、`review-actions-template.csv` 已更新到本地时间 `2026-07-08 18:24:12`。

下一步可以选择两条路径：

- 接真实 Baserow/Notion，把本地 `status` 流转迁移到在线表格。
- 继续强化本地审核闭环，增加“导入 review-actions-template.csv 并更新 SQLite 状态”的脚本。

## 18. 本地审核状态回写闭环

审核页面生成的 `review-actions-template.csv` 可以作为人工状态动作表：

```text
data\review-portal\review-actions-template.csv
  ↓
填写 next_status 和 review_note
  ↓
scripts\apply_review_actions.py
  ↓
更新 data\topic-database\topic-candidates.sqlite
  ↓
写入 topic_review_actions 审核动作日志
  ↓
重新生成审核入口和通知摘要
```

允许状态：

```text
待评估
人工复核
可写
已写
放弃
```

安全验证命令：

```powershell
python scripts\apply_review_actions.py --dry-run
```

真实应用命令：

```powershell
python scripts\apply_review_actions.py
```

设计原则：

- 空 `next_status` 行会跳过，不会误更新。
- `current_status` 与数据库不一致时默认拒绝，避免旧 CSV 覆盖新状态。
- `--dry-run` 不修改真实数据库，也不创建审核日志表。
- 真实应用会写入 `topic_review_actions`，保留 `previous_status`、`next_status`、`review_note` 和来源 CSV。

当前 n8n workflow：

```text
n8n-workflows\topic-review-actions-smoke.workflow.json
```

当前验证状态（2026-07-08）：

- workflow 已导入本地 n8n，ID 为 `RedBookTopicReviewActionsSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- 真实数据库 dry-run 结果为 `action_rows=58`、`skipped_blank_next_status=58`。
- 真实数据库仍保持 `待评估=58`，没有被 dry-run 修改。
- smoke 复制库测试已成功应用 3 条状态变更：`可写=1`、`人工复核=1`、`放弃=1`，并写入 3 条审核动作日志。

下一步可以把这个状态闭环迁移到 Baserow/Notion，或者先做定时通知，让 n8n 每天生成审核摘要。

## 19. 每日审核摘要和定时生成

在真实消息渠道接入前，先让 n8n 每天生成本地日报文件：

```text
Daily 09:00 Schedule
  ↓
刷新审核入口 generate_review_portal.py
  ↓
dry-run 校验审核动作 apply_review_actions.py --dry-run
  ↓
生成日报 generate_daily_digest.py
  ↓
data\daily-digest\latest-digest.md
  ↓
data\daily-digest\latest-digest.json
  ↓
data\daily-digest\<YYYY-MM-DD>-digest.md
```

当前脚本：

```powershell
python scripts\generate_daily_digest.py
```

当前 n8n workflow：

```text
n8n-workflows\daily-review-digest-schedule.workflow.json
```

设计原则：

- 定时任务先生成本地文件，再通过飞书自定义机器人外发日报。
- 每次生成日报前先刷新审核入口和 dry-run 状态回写，保证摘要基于最新本地状态。
- 日报包含审核队列数量、兽医边界数量、状态分布、主题分布、风险分布和优先审核候选。
- workflow 保留 Manual Trigger，方便 CLI 和 UI 手动验证。

当前验证状态（2026-07-09）：

- workflow 已导入本地 n8n，ID 为 `RedBookDailyReviewDigestSchedule`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- `latest-digest.md` 和 `latest-digest.json` 已更新到本地时间 `2026-07-09 10:54:22`。
- workflow 已发布并在 n8n 重启后激活，数据库显示 `active=1`。
- 当前定时规则为 Asia/Shanghai 每天 `09:00`。
- 当前 workflow 已追加 `Send Feishu Digest` 节点，飞书发送在本地时间 `2026-07-09 10:54:23` 返回 `result=sent`、`http_status=200`。
- 当前日报统计：选题库 58 条，审核队列 58 条，需兽医边界 34 条，审核动作表未填写 58 行。

飞书已经接入为当前真实通知渠道，后续如果需要再扩展企业微信、邮件、Telegram 或其他消息入口。

## 20. 飞书通知接入

当前已接入飞书自定义机器人。脚本默认仍保持 dry-run，但 n8n 真实发送节点显式使用 `--send`：

```text
Manual Trigger
  ↓
生成日报 generate_daily_digest.py
  ↓
飞书真实发送 send_feishu_digest.py --send
  ↓
data\feishu\latest-payload.json
  ↓
data\feishu\latest-send-summary.json
  ↓
data\feishu\latest-send-summary.md
```

当前脚本：

```powershell
python scripts\send_feishu_digest.py
python scripts\send_feishu_digest.py --send
python scripts\send_feishu_digest.py --write-env-template
```

当前 n8n workflow：

```text
n8n-workflows\feishu-digest-notification.workflow.json
```

配置文件位置：

```text
data\feishu\feishu.env
```

配置字段：

```text
FEISHU_WEBHOOK_URL=
FEISHU_WEBHOOK_SECRET=
FEISHU_KEYWORD=RedBook
```

设计原则：

- `send_feishu_digest.py` 默认只生成 payload 和发送摘要，不真实发送，避免本地误触发。
- 真实发送必须显式加 `--send`；当前 n8n 飞书节点已经使用该参数。
- webhook 和签名密钥只放在 `data\feishu\feishu.env` 或环境变量中，不写入 workflow JSON。
- 如果飞书机器人开启签名校验，填写 `FEISHU_WEBHOOK_SECRET`。
- 如果飞书机器人开启关键词校验，保留 `FEISHU_KEYWORD=RedBook`，脚本会保证消息文本包含该关键词。

当前验证状态（2026-07-09）：

- `scripts\send_feishu_digest.py` 已通过 `python -m py_compile`。
- 本地 dry-run 已成功生成 `data\feishu\latest-payload.json`、`latest-send-summary.json` 和 `latest-send-summary.md`。
- workflow 已导入本地 n8n，ID 为 `RedBookFeishuDigestNotification`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- workflow 当前保持未激活，数据库显示 `active=0`。
- 真实配置文件 `data\feishu\feishu.env` 已存在，包含 webhook、签名密钥和关键词配置。
- 本地脚本真实发送在 `2026-07-09 10:50:37` 成功，返回 `result=sent`、`http_status=200`。
- 独立飞书 n8n workflow 真实发送在 `2026-07-09 10:52:39` 成功，返回 `result=sent`、`http_status=200`。
- 每日定时 workflow 已追加飞书发送节点，完整链路验证在 `2026-07-09 10:54:23` 成功。

当前上线状态：

1. 飞书自定义机器人 webhook 已配置到本地 `data\feishu\feishu.env`。
2. 独立脚本和 n8n workflow 都已通过真实发送验证。
3. 已激活的每日 09:00 日报 workflow 已接入飞书发送。
4. n8n 服务已重启，健康接口返回正常；下一次自动发送时间为 Asia/Shanghai `2026-07-10 09:00`。

## 21. 飞书多维表格选题库同步 dry-run（主线）

外部选题库主线从 Baserow 改为飞书多维表格。原因是当前项目已经接入飞书机器人通知，后续选题审核、状态流转和日报通知都放在飞书体系内更顺：

```text
data\topic-ingestion\baserow-topic-candidates.json
  ↓
sync_feishu_bitable_topics.py
  ↓
data\feishu-bitable\latest-sync-summary.json
  ↓
data\feishu-bitable\latest-sync-summary.md
```

当前脚本：

```powershell
python scripts\sync_feishu_bitable_topics.py
python scripts\sync_feishu_bitable_topics.py --sync
python scripts\sync_feishu_bitable_topics.py --write-env-template
```

当前 n8n workflow：

```text
n8n-workflows\feishu-bitable-topic-sync-smoke.workflow.json
```

配置文件位置：

```text
data\feishu-bitable\feishu-bitable.env
```

配置字段：

```text
FEISHU_BASE_URL=https://open.feishu.cn
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_BITABLE_APP_TOKEN=
FEISHU_BITABLE_TABLE_ID=
FEISHU_UPSERT_FIELD=candidate_id
FEISHU_PAGE_SIZE=500
```

飞书多维表格字段配置文件：

```text
data\feishu-bitable\field-mapping.md
```

设计原则：

- 默认 dry-run，只读取本地 `baserow-topic-candidates.json` 并生成同步摘要。
- 真实写入必须显式加 `--sync`。
- 飞书自建应用凭据、app token 和 table id 只放在 `data\feishu-bitable\feishu-bitable.env` 或环境变量中，不写入 workflow JSON。
- 使用 `candidate_id` 作为 upsert key。
- 更新已有记录时默认保留远端 `status`，避免覆盖飞书表格中的人工审核状态；只有显式加 `--overwrite-status` 才覆盖。
- `source_url`、`created_at` 和本地路径字段建议在飞书多维表格中使用文本字段，降低 API 字段格式差异。

当前验证状态（2026-07-10）：

- `scripts\sync_feishu_bitable_topics.py` 已通过 `python -m py_compile`。
- 本地 dry-run 成功读取 58 条选题候选，去重后 58 条，重复输入 0 条。
- dry-run 输出已写入 `data\feishu-bitable\latest-sync-summary.json` 和 `latest-sync-summary.md`。
- workflow 已导入本地 n8n，ID 为 `RedBookFeishuBitableTopicSyncSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- workflow 当前保持未激活，数据库显示 `active=0`。
- 当前没有配置飞书自建应用凭据、`FEISHU_BITABLE_APP_TOKEN` 和 `FEISHU_BITABLE_TABLE_ID`，因此没有写入外部飞书多维表格。

上线真实飞书多维表格同步的顺序：

1. 在飞书开放平台创建自建应用，拿到 `app_id` 和 `app_secret`。
2. 给应用开通多维表格读写相关权限，并在飞书后台发布/启用权限。
3. 创建飞书多维表格，按 `data\feishu-bitable\field-mapping.md` 创建字段。
4. 从多维表格 URL 中拿到 `app_token` 和 `table_id`。
5. 把 `data\feishu-bitable\feishu-bitable.env.example` 复制为 `data\feishu-bitable\feishu-bitable.env` 并填写真实值。
6. 先执行 `python scripts\sync_feishu_bitable_topics.py --sync --limit 3` 验证 3 条样本。
7. 确认字段、状态和中文内容正常后，再执行 `python scripts\sync_feishu_bitable_topics.py --sync` 全量同步。
8. 全量同步稳定后，把 n8n 节点命令从 dry-run 改为 `python "D:\AUnityProject\RedBook\scripts\sync_feishu_bitable_topics.py" --sync`。

## 22. Baserow 选题库同步 dry-run（备选）

Baserow 同步层保留为备选路径。除非明确要使用 Baserow Cloud 或自建 Baserow，否则优先使用飞书多维表格。当前 Baserow 仍只完成 dry-run，避免在 token、表结构或字段未确认前误写真实表：

```text
data\topic-ingestion\baserow-topic-candidates.json
  ↓
sync_baserow_topics.py
  ↓
data\baserow\latest-sync-summary.json
  ↓
data\baserow\latest-sync-summary.md
```

当前脚本：

```powershell
python scripts\sync_baserow_topics.py
python scripts\sync_baserow_topics.py --sync
python scripts\sync_baserow_topics.py --write-env-template
```

当前 n8n workflow：

```text
n8n-workflows\baserow-topic-sync-smoke.workflow.json
```

配置文件位置：

```text
data\baserow\baserow.env
```

配置字段：

```text
BASEROW_API_URL=https://api.baserow.io
BASEROW_TOKEN=
BASEROW_TABLE_ID=
BASEROW_UPSERT_FIELD=candidate_id
BASEROW_READ_PAGE_SIZE=200
```

设计原则：

- 默认 dry-run，只读取本地 `baserow-topic-candidates.json` 并生成同步摘要。
- 真实写入必须显式加 `--sync`。
- Baserow token 和 table id 只放在 `data\baserow\baserow.env` 或环境变量中，不写入 workflow JSON。
- 使用 `candidate_id` 作为 upsert key。
- 更新已有行时默认保留远端 `status`，避免覆盖在线人工审核状态；只有显式加 `--overwrite-status` 才覆盖。
- 真实同步前先按 `data\topic-ingestion\field-mapping.md` 创建 Baserow 字段。

当前验证状态（2026-07-09）：

- `scripts\sync_baserow_topics.py` 已通过 `python -m py_compile`。
- 本地 dry-run 成功读取 58 条选题候选，去重后 58 条，重复输入 0 条。
- dry-run 输出已写入 `data\baserow\latest-sync-summary.json` 和 `latest-sync-summary.md`。
- workflow 已导入本地 n8n，ID 为 `RedBookBaserowTopicSyncSmoke`。
- CLI 已成功执行一次，n8n 执行记录为 `status = success`。
- workflow 当前保持未激活，数据库显示 `active=0`。
- 当前没有配置 `BASEROW_TOKEN` 和 `BASEROW_TABLE_ID`，因此没有写入外部 Baserow。

上线真实 Baserow 同步的顺序：

1. 在 Baserow 创建 topic candidates 表。
2. 按 `data\topic-ingestion\field-mapping.md` 创建字段，至少包含全部脚本字段。
3. 创建 Baserow database token，并授予目标表读写权限。
4. 把 `data\baserow\baserow.env.example` 复制为 `data\baserow\baserow.env` 并填写真实值。
5. 先执行 `python scripts\sync_baserow_topics.py --sync --limit 3` 验证 3 条样本。
6. 确认字段、状态和中文内容正常后，再执行 `python scripts\sync_baserow_topics.py --sync` 全量同步。
7. 全量同步稳定后，把 n8n 节点命令从 dry-run 改为 `python "D:\AUnityProject\RedBook\scripts\sync_baserow_topics.py" --sync`。
